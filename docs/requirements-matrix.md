# Матрица требований

**Тема:** Информационная система бронирования спортивного зала
**Студент:** Закиров Рамзиль Ангамович · **Группа:** ТРИС-2-23
**Лабораторная работа №2**

Каждый критерий приёмки из ЛР1 (`docs/lab-01-requirements.md`) связан с полем модели данных и запросом API. Если строку нельзя заполнить — модель или ТЗ противоречивы.

| Требование ЛР1 | Поле или сущность | Запрос | Критерий |
|---|---|---|---|
| Создать объект: преподаватель создаёт бронь, появляется запись со статусом `New` и уникальным номером | `Booking`, `title`, `roomId`, `date`, `time`, `description` | `POST /api/bookings` | 1 |
| Назначить исполнителя: администратор назначает себя отдельным действием, статус остаётся `New` | `assigneeUserId` | `PATCH /api/bookings/{id}/assignee` | 2 |
| Исполнитель перевёл в работу: статус становится `InProgress`, чужой не может | `status` | `PATCH /api/bookings/{id}/status` | 3 |

## Дополнительное покрытие функциональных требований

| Требование | Поле или сущность | Запрос |
|---|---|---|
| ФТ-5: просмотр списка и одной брони | `Booking` целиком | `GET /api/bookings`, `GET /api/bookings/{id}` |
| ФТ-8: завершение обработки | `status` = `Closed` | `PATCH /api/bookings/{id}/status` |
| ФТ-9: отмена брони (без удаления) | `status` = `Cancelled` | `PATCH /api/bookings/{id}/status` |
| ФТ-10: запрет смены статуса чужой брони | `assigneeUserId` + роль пользователя | `PATCH …/status` → `409` |
| ФТ-4: авто-присвоение `id` и `number` | `id`, `number` | ответ `201` на `POST /api/bookings` |
| НФТ-1: сохранение после перезапуска | все сущности | PostgreSQL, сервер — единственный писатель |
| НФТ-2: проверка ролей | `User.role` | все `POST`/`PATCH` на сервере |
